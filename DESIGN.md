---
title: TODOROK Design System
product: TODOROK
status: approved
version: 1.2.10
created: 2026-09-01
updated: 2026-09-01
owner: 김민준
---

# TODOROK 디자인 시스템

## 1. 목적

이 문서는 토도록 웹과 향후 모바일 앱의 시각 언어, 화면 구조, 상호작용과 접근성 기준을 정의한다. 제품 요구사항은 `docs/PRD.md`, 구현 세부 디자인은 이 문서를 기준으로 한다.

토도록은 처음 보면 간결한 달력 기반 할 일 앱이고, 운동·공부·클라이밍 할 일을 완료할 때 필요한 기록 기능이 자연스럽게 연결되어야 한다. 오늘 화면에 기록 시스템의 복잡도를 미리 노출하지 않는다.

## 2. 디자인 원칙

1. **오늘이 먼저다.** 앱을 열면 실제 오늘 날짜와 할 일이 가장 먼저 보여야 한다.
2. **체크는 가볍다.** 일반 할 일은 한 번에 완료하고 기록형 할 일만 전용 기록 흐름으로 이동한다.
3. **복잡도는 늦게 연다.** 추가 옵션과 기록 필드는 사용자가 필요로 할 때만 펼친다.
4. **평평하게 구성한다.** 기본 화면은 배경과 구분선으로 나누고 카드 남용을 피한다.
5. **색은 작은 신호다.** 차콜과 무채색이 중심이며 파스텔은 카테고리와 의미 표시에만 사용한다.
6. **계획과 실제는 한 항목이다.** 기록 저장 후 별도 항목을 추가하지 않고 기존 할 일이 완료 기록으로 변한다.
7. **모바일과 데스크톱은 같은 제품이다.** 배치만 달라지고 메뉴·용어·상태·행동은 유지한다.

## 3. 시각 방향

### 3.1 핵심 인상

- 방향명: `Monochrome + Pastel`
- 기본 인상: 밝고 조용하며 밀도 있는 개인 일정 도구
- 중심 요소: 주·월 달력, 평평한 할 일 행, 얇은 구분선, 작은 카테고리 색상
- 배제 요소: 과도한 둥근 카드, 유리 효과, 그라데이션 장식, 큰 캐릭터 빈 상태, 통계 카드가 먼저 보이는 대시보드

### 3.2 표면

- 오늘 화면과 목록은 기본 배경 위에 직접 배치한다.
- 항목 사이에는 1px 구분선을 사용한다.
- 카드형 표면은 타이머, 단일 기록 요약, 확인이 필요한 모달에만 사용한다.
- 그림자는 데스크톱의 큰 작업 패널과 떠 있는 모달에만 약하게 사용한다.
- 모서리 반경은 작은 제어 요소 8px, 패널 16px, 모바일 프레임 외곽 32px을 기준으로 한다.

## 4. 색상

### 4.1 라이트 테마

| 토큰 | 값 | 용도 |
|---|---:|---|
| `color.bg.canvas` | `#FFFFFF` | 앱 기본 배경 |
| `color.bg.subtle` | `#F5F6F6` | 비활성·보조 표면 |
| `color.bg.elevated` | `#FFFFFF` | 모달·큰 패널 |
| `color.text.primary` | `#232527` | 제목·본문 |
| `color.text.secondary` | `#74797E` | 보조 정보 |
| `color.border.default` | `#E5E8EA` | 구분선·테두리 |
| `color.action.primary` | `#282B2D` | 선택 날짜·주요 버튼 |
| `color.focus` | `#2563EB` | 키보드 초점선 |
| `color.danger` | `#C84B4B` | 삭제·중단 위험 |

### 4.2 다크 테마

| 토큰 | 값 | 용도 |
|---|---:|---|
| `color.bg.canvas` | `#181A1C` | 앱 기본 배경 |
| `color.bg.subtle` | `#24272A` | 비활성·보조 표면 |
| `color.bg.elevated` | `#1D2022` | 모달·큰 패널 |
| `color.text.primary` | `#F5F5F4` | 제목·본문 |
| `color.text.secondary` | `#B0B4B7` | 보조 정보 |
| `color.border.default` | `#383C3F` | 구분선·테두리 |
| `color.action.primary` | `#F0F1F1` | 선택 상태·주요 버튼 |
| `color.focus` | `#60A5FA` | 키보드 초점선 |
| `color.danger` | `#EF7474` | 삭제·중단 위험 |

- 다크 테마의 선택 날짜는 밝은 `color.action.primary` 면을 사용하지 않는다.
- 주간·월간 선택 날짜는 `#303438` 배경과 중간 회색 외곽선, 밝은 숫자로 표시해 큰 흰색 면을 피한다.

### 4.3 카테고리 팔레트

사용자는 다음 8색 중 하나를 고른다. 자유 색상 선택기는 제공하지 않는다.

| 이름 | 라이트 | 다크 |
|---|---:|---:|
| 코랄 | `#E99680` | `#D48A78` |
| 스카이 | `#83ABC4` | `#7DA6BF` |
| 머스터드 | `#C8A75F` | `#BEA366` |
| 민트 | `#7FAE9D` | `#78A696` |
| 라벤더 | `#A99AC7` | `#9E91BD` |
| 로즈 | `#D58FA1` | `#C8899A` |
| 틸 | `#6FA9A9` | `#68A0A0` |
| 슬레이트 | `#8796A5` | `#82919F` |

- 기본 매핑은 운동 코랄, 공부 스카이, 클라이밍 머스터드다.
- 카테고리 색은 점, 얇은 강조선, 작은 아이콘에 사용한다.
- 긴 본문이나 주요 버튼 배경으로 사용하지 않는다.
- 모든 상태에는 아이콘이나 텍스트 라벨을 함께 제공한다.

### 4.4 테마 선택

- 설정에서 `시스템 설정 · 라이트 · 다크`를 제공한다.
- 기본값은 시스템 설정이다.
- 사용자가 직접 선택한 값은 계정 설정으로 저장한다.
- 브라우저가 테마를 바꾸더라도 입력 중인 값과 타이머 상태를 초기화하지 않는다.

## 5. 타이포그래피

### 5.1 글꼴

- 기본 UI: `Pretendard Variable`
- 타이머 숫자: `Inter 700` (`Inter Variable` 가변 글꼴의 700 weight)
- 대체 글꼴: `Apple SD Gothic Neo`, `Segoe UI`, `sans-serif`
- 배포 번들에는 필요한 WOFF2만 포함하고 `font-display: swap`을 사용한다.
- 타이머에는 `font-variant-numeric: tabular-nums`와 contextual alternates를 적용한다.

### 5.2 크기와 행간

| 역할 | 모바일 | 데스크톱 | 굵기·행간 |
|---|---:|---:|---|
| 화면 날짜·월 제목 | 27px | 28px | 800 / 1.25 |
| 화면 제목 | 20px | 22px | 750 / 1.3 |
| 구역 제목 | 15px | 16px | 750 / 1.4 |
| 할 일 제목 | 15px | 15px | 650 / 1.3 |
| 본문 | 14px | 14px | 450 / 1.55 |
| 보조 정보 | 12px | 12px | 450 / 1.4 |
| 하단 메뉴 | 11px | 12px | 600 / 1.2 |
| 타이머 숫자 | 72px | 78px | 700 / 1.0 |
| 타이머 현재 구간 | 16px | 16px | 800 / 1.3 |
| 타이머 상세 | 15px | 15px | 600 / 1.4 |
| 타이머 다음 구간 | 14px | 14px | 500 / 1.7 |

- 본문과 보조 정보는 브라우저 확대 200%에서 내용이나 조작이 손실되지 않아야 한다.
- 타이머 숫자는 너비가 변하지 않아 매초 레이아웃이 흔들리지 않아야 한다.

## 6. 간격과 크기

기본 간격은 4px 배수다.

| 토큰 | 값 |
|---|---:|
| `space.1` | 4px |
| `space.2` | 8px |
| `space.3` | 12px |
| `space.4` | 16px |
| `space.5` | 20px |
| `space.6` | 24px |
| `space.8` | 32px |
| `space.10` | 40px |

- 모바일 좌우 여백: 18px
- 데스크톱 패널 내부 여백: 28px
- 할 일 행 최소 높이: 56px
- 주요 터치 영역 최소 크기: 44×44px
- 체크 원: 모바일 23px, 데스크톱 23px
- 카테고리 점: 8px

## 7. 내비게이션과 반응형

### 7.1 메뉴

다음 다섯 메뉴를 항상 같은 순서로 사용한다.

`오늘 · 운동 · 공부 · 클라이밍 · 설정`

1. 오늘
2. 운동
3. 공부
4. 클라이밍
5. 설정

모바일은 하단 탭, 데스크톱은 왼쪽 사이드바다. 선택 상태는 색뿐 아니라 아이콘과 굵은 라벨로 표시한다.

### 7.2 구간

- `0–767px`: 모바일 한 열, 고정 하단 탭
- `768–1023px`: 가운데 정렬 한 열, 하단 탭 유지
- `1024px 이상`: 왼쪽 사이드바와 작업 영역 사용

### 7.3 데스크톱 오늘 화면

- 왼쪽: 주간·월간 달력
- 오른쪽: 선택 날짜 제목, 할 일, 날짜별 스티키노트
- 월간 달력은 `repeat(7, minmax(0, 1fr))`로 날짜 7열을 패널 안에 고정한다.
- 체크 원과 카테고리 점은 좁아져도 축소하거나 숨기지 않는다.
- 화면이 좁아지면 두 패널을 줄인 뒤 1024px 아래에서 한 열로 전환한다.
- 1023px 이하 한 열에서는 달력과 선택 날짜 목록을 각각 테두리가 있는 독립 영역으로 구분한다.
- 한 열의 달력과 선택 날짜 영역은 같은 최대 폭을 사용한다.
- 두 영역을 감싸는 바깥 레이아웃에는 별도 배경색을 사용하지 않는다.
- 선택 날짜 영역 아래와 고정 하단 메뉴 사이에는 최소 24px 여백을 둔다.

## 8. 오늘 화면

### 8.1 진입

- 새로 열면 오늘 탭의 실제 오늘 날짜를 표시한다.
- 마지막 탭이나 과거 선택 날짜는 복원하지 않는다.
- 마지막 `주 · 월` 보기 선택만 기기에 기억한다.
- 자동 이월은 팝업이나 토스트 없이 목록에 반영한다.
- 이월 여부는 개별 할 일 상세 이력에서 확인한다.
- 오늘 화면 본문에는 `TODAY`, `오늘` 또는 선택 날짜를 반복하는 별도 화면 제목을 사용하지 않는다.
- `할 일은 가볍게, 기록은 필요할 때 자세하게.` 문구는 모바일 상단 `TODOROK` 바로 아래의 작은 브랜드 보조 문구로만 표시한다.
- 날짜 정보는 달력과 선택 날짜 영역에만 표시한다.
- 모바일 `TODOROK` 이름은 18px 수준으로 표시하고 보조 문구보다 명확한 위계를 갖게 한다.
- 모바일 상단 브랜드 영역은 별도 구분선이나 강한 배경 차이 없이 본문과 자연스럽게 이어진다.

### 8.2 달력

- 헤더에서 `주 · 월`을 즉시 전환한다.
- 어느 보기에서든 선택 날짜의 할 일은 같은 위치에 표시한다.
- `오늘` 버튼은 연·월 제목 바로 옆의 작은 제어 요소로 고정하고 선택 날짜가 실제 오늘이면 비활성화한다.
- 연·월 제목은 9월에서 10월처럼 글자 수가 달라져도 주·월 전환과 오늘 버튼 위치가 움직이지 않도록 고정 폭을 확보한다.
- 월간 보기에서는 연·월 제목 양옆에 이전 달·다음 달 버튼을 제공한다.
- 이전·다음 달 이동은 9월→10월, 11월→12월과 12월→다음 해 1월을 같은 규칙으로 처리한다.
- 주간 보기에서는 월 이동 버튼의 공간을 유지하되 비활성화해 주·월 전환 시 제목과 버튼 위치가 움직이지 않게 한다.
- 주간 보기의 각 날짜 아래에는 `할 일 · 운동 · 공부 · 클라이밍` 계획을 카테고리 색상 조각으로 표시한다.
- 날짜·카테고리 셀은 `완료 수 / 전체 수` 비율만큼 해당 색으로 채운다. 여러 항목 중 일부만 완료하면 채움 면적이 진행률에 따라 늘어난다.
- 주간 분포 영역에는 별도 제목·설명 문구와 숫자를 표시하지 않고 카테고리 이름과 색 채움만 남긴다.
- 전체 수와 완료 수는 화면에 보이지 않는 접근 가능한 이름으로 제공한다.
- 색상 조각에는 접근 가능한 설명을 제공하고 색만으로 완료 여부를 구분하지 않는다.
- 데스크톱 주간 패널은 날짜만 남은 큰 빈 영역을 만들지 않고, 카테고리별 주간 분포와 날짜별 완료 수로 공간을 사용한다.
- 월간 보기의 날짜 칸은 무채색을 유지하고 숫자 아래의 짧은 막대에만 완료 카테고리 색을 표시한다.
- 같은 날짜에 여러 카테고리의 완료 항목이 있으면 짧은 막대 내부만 완료 카테고리 수만큼 균등 분할한다.
- 미완료 일정은 색상 막대를 표시하지 않으며 완료 개수 숫자도 표시하지 않는다.
- 선택 날짜는 차콜 배경으로 강조하고 완료 색상 막대는 선택 배경 위에서도 유지한다.

### 8.3 할 일 행

선택 날짜의 항목은 항목이 없어도 다음 네 그룹을 고정 순서로 표시한다.

1. 할 일
2. 운동
3. 공부
4. 클라이밍

- 각 그룹 제목에는 카테고리 색상, 완료 수와 전체 수, 작은 `+` 추가 버튼을 함께 표시한다.
- 화면 전체에 떠 있는 공통 추가 버튼은 사용하지 않는다.
- 비어 있는 그룹은 안내 문장 없이 제목과 추가 버튼만 표시한다.
- 그룹 제목은 옅은 배경 또는 명확한 경계로 목록 행과 구분한다.
- 그룹 안의 행은 왼쪽부터 다음 순서로 배치한다.

1. 체크 원
2. 제목과 보조 정보
3. 필요할 때만 보조 상태 또는 더보기

- 일반 할 일은 체크 즉시 완료한다.
- 운동·공부·클라이밍 할 일은 체크 시 해당 탭의 기록 화면으로 이동한다.
- 기록 화면에서 취소하거나 이탈하면 미완료 상태를 유지한다.
- 기록 저장 성공 시 완료 처리하고 오늘 화면으로 돌아온다.
- 시간이 있으면 기존 할 일이 시간 블록으로 변한다.
- 시간이 없으면 완료 행에 수행량 요약을 표시한다.
- 별도의 기록 항목을 추가해 같은 내용을 중복 표시하지 않는다.
- 각 그룹의 마지막 행에는 하단 구분선을 표시하지 않는다.

### 8.4 빠른 추가

- 오늘 화면의 `할 일` 그룹 옆 `+`를 누르면 제목 한 줄 입력을 먼저 연다.
- 운동·공부·클라이밍 그룹 옆 `+`는 각 도메인의 일정 추가 화면을 연다.
- 제목 입력 후 바로 저장할 수 있다.
- 날짜·반복·분류·시간·개수는 `옵션`에서 펼친다.
- 기본 결과는 체크형 할 일이다.

### 8.5 스티키노트

- 선택 날짜의 할 일 목록 아래에 한 줄 미리보기를 표시한다.
- 누르면 같은 화면에서 편집 영역이 펼쳐진다.
- 저장 중, 저장됨, 저장 실패를 텍스트와 아이콘으로 표시한다.
- 할 일 목록과 구분되는 옅은 배경, 외곽선과 내부 여백을 사용해 하나의 메모 영역으로 보이게 한다.

## 9. 도메인 탭과 기록

### 9.1 첫 화면

운동·공부·클라이밍 탭은 다음 순서로 구성한다.

1. 오늘 할 일·루틴
2. 기록 시작
3. 최근 기록

누적 수치와 차트는 별도 통계 화면에 두고 첫 화면을 대시보드 카드로 채우지 않는다.

- `오늘 항목`, `등록 방식·카테고리`, `최근 기록`은 각각 외곽선과 여백을 가진 큰 영역으로 구분한다.
- 큰 영역 내부의 개별 행마다 얇은 실선을 반복하지 않고 간격과 배경 차이로 구분한다.

- 운동·공부·클라이밍 탭 본문에는 영문 eyebrow, 큰 탭 이름과 설명을 반복하지 않는다.
- 도메인 화면 최상단에는 주간 대신 선택 월 기준 요약을 표시한다.
- 좌측 상단에 이전 달·연월·다음 달, 우측 상단에 `+ 추가`와 `지난 기록`을 둔다.
- 완료·수행 또는 집중 시간·등록 수치는 둘째 줄 전체 폭을 같은 너비의 세 칸으로 나눠 배치한다.
- 집계 값은 18px 이상으로 강조하고 지표 라벨은 보조 크기로 표시한다.
- 우측 상단의 추가·지난 기록 버튼은 34–36px 높이의 작은 제어 요소로 표시한다.
- 월을 이동하면 세 수치도 선택 월의 집계로 변경하며 운동·공부·클라이밍 탭은 같은 선택 월을 공유한다.
- 첫 번째 `오늘 운동·오늘 공부·오늘 클라이밍` 영역은 제목과 항목만 표시한다.
- 도메인 탭에서 만든 항목도 오늘 화면의 같은 Task로 연결하며 별도 일정 복사본을 만들지 않는다.
- 도메인의 오늘 항목은 별도 `기록` 버튼 대신 오늘 화면과 같은 체크 원을 사용하고, 체크 시 해당 기록 화면을 연다.

### 9.2 도메인별 할 일 추가

카테고리·기록 템플릿의 생성과 날짜가 있는 할 일 생성은 서로 다른 작업이다.

- `카테고리 관리` 또는 `기록 항목 관리`에서는 이름, 필드, 단위와 순서만 저장하고 일정을 만들지 않는다.
- `+ 추가`에서는 기존 카테고리·기록 템플릿을 선택한 뒤 날짜와 반복을 지정해 Task를 만든다.
- 관리 화면에서 정의를 저장한 뒤 원래의 일정 추가 화면으로 돌아와 새 정의를 선택할 수 있다.

#### 운동

- 선택 화면에서는 `프로그램·챌린지`를 항상 위에, `자유 운동`을 아래에 고정한다.
- 프로그램을 선택하면 먼저 푸시업·풀업 등 종목을 고르고 그 종목에 제공되는 프로그램을 선택한다.
- 자유 운동은 기존 종목을 고르거나 새 종목을 만들고, 횟수·세트·시간·거리·무게 중 필요한 기록 단위를 선택한다.
- 자유 운동의 기록 항목과 단위는 공부 카테고리처럼 별도의 기록 템플릿에서 추가·삭제·정렬할 수 있다.
- 푸시업과 풀업처럼 내장 프로그램이 있는 종목은 `일반 기록`과 `프로그램 참여`를 함께 제공한다.
- 프로그램 참여를 고르면 프로그램 이름, 현재 주차·회차, 권장 운동량과 시작 단계 설정을 미리 보여준다.
- 오늘 목록에는 `프로그램 · 3주차 1일` 또는 `자유 운동 · 횟수 기록`처럼 등록 방식을 표시한다.

#### 공부

- 공부 카테고리를 먼저 선택하고 카테고리에 정의된 기록 항목과 단위를 추가 화면에서 미리 보여준다.
- `카테고리 관리`는 일정 추가와 분리하며, 새 카테고리를 저장해도 할 일은 자동 생성하지 않는다.
- 알고리즘의 문제 수·소요 시간, 독서의 쪽 수, 영어의 단어 수처럼 카테고리마다 서로 다른 선택 항목을 사용한다.
- 오늘 목록에는 `알고리즘 · 문제 수·소요 시간`처럼 카테고리와 기록 기준을 함께 표시한다.
- 공부 카테고리 영역 안에서는 동일한 공부 색상 점을 항목마다 반복하지 않고 이름과 기록 항목만 표시한다.

#### 클라이밍

- 선택 화면에서는 내장 `크림프 루틴`을 위에, `자유 행보드`와 `클라이밍 세션`을 아래에 고정한다.
- 크림프 루틴은 내장 타이머와 그립 순서를 연결하고, 자유 행보드는 그립·운동·휴식 시간을 직접 정한다.
- 클라이밍 세션은 시간·장소·완등 수처럼 사용자가 입력한 항목만 기록한다.
- 자유 행보드와 클라이밍 세션도 별도의 기록 템플릿에서 남길 항목과 단위를 직접 추가·삭제·정렬할 수 있다.
- 768px 이상에서는 프로그램·자유 운동과 클라이밍 유형 카드를 2열로, 모바일에서는 우선순서를 유지한 한 열로 표시한다.

### 9.3 오늘 화면과 도메인 탭의 연결

- 오늘 화면의 `+`는 일반 할 일을 기본으로 하되 분류에서 운동·공부·클라이밍을 선택하면 해당 도메인 추가 화면으로 이어진다.
- 도메인 탭의 `+ 추가`는 해당 유형을 미리 선택한 상태로 같은 Task 생성 흐름을 연다.
- 생성 완료 후 도메인 탭의 오늘 목록과 오늘 화면의 해당 카테고리 그룹에 동시에 표시한다.

### 9.4 지난 기록

- 각 도메인 탭 상단에 `지난 기록 추가`를 둔다.
- `날짜 선택 → 할 일 또는 루틴 선택 → 기존 기록 폼` 순서다.
- 저장 후 오늘 탭으로 이동해 기록한 과거 날짜를 선택한 상태로 보여준다.
- 날짜 헤더의 `오늘` 버튼으로 실제 오늘에 돌아간다.

### 9.5 동기화 상태

- 기록 저장 후 오늘 화면으로 즉시 복귀한다.
- 일정 완료 반영 전에는 해당 할 일에 작은 `반영 중` 상태를 표시한다.
- 장시간 실패하면 `기록됨 · 일정 반영 재시도`를 표시한다.
- 기록 저장과 일정 동기화 성공 여부를 하나의 성공 표시로 합치지 않는다.

### 9.6 기록 화면

- 뒤로 가기는 본문 위에 단독 버튼으로 띄우지 않고 화면 제목 행의 왼쪽에 작은 아이콘 버튼으로 배치한다.
- 시작·종료 시간은 브라우저 기본 시간 입력 대신 `오전·오후`, `1–12시`, `00–55분`을 분리해 선택한다.
- 분 선택은 5분 단위만 제공한다.
- 목록·최근 기록·도메인 항목 행은 모바일에서 52–58px 높이를 기준으로 하고 아이콘·날짜·본문 사이 간격을 8px 안팎으로 유지한다.
- 하단 메뉴는 아이콘과 라벨을 하나의 세로 flex 묶음으로 정렬하고 둘 사이 간격을 2px로 제한한다.

## 10. 클라이밍 타이머

### 10.1 정보 위계

1. 현재 상태: `운동` 또는 `휴식`
2. 남은 시간: 큰 Inter 숫자
3. 현재 그립·세트·부하
4. 일시정지와 현재 구간 건너뛰기
5. 다음 구간 목록

- 운동과 휴식은 색, 문구, 소리로 함께 구분한다.
- 숫자만 보아도 멀리서 남은 시간을 읽을 수 있어야 한다.
- 콜론은 숫자 몸통의 시각적 중앙에 위치해야 한다.
- 현재 상태와 남은 시간은 화면 확대나 좁은 폭에서도 잘리지 않는다.

### 10.2 구간 전환

- 큰 시각 변화, 짧은 소리, 지원 기기의 진동을 제공한다.
- 소리와 진동은 각각 끌 수 있다.
- 동작 감소 설정에서는 확대·튕김 효과를 제거하고 색·문구·소리 중심으로 알린다.
- 화면 잠금 방지를 지원하지 못하거나 실패하면 타이머 시작 전에 안내한다.

## 11. 아이콘

### 11.1 공통 규칙

- 선형 아이콘을 기본으로 한다.
- 기본 선 굵기: 1.75px
- 끝과 모서리: 둥근 마감
- 기본 크기: 24px, 보조 크기: 16px, 강조 크기: 32px
- 작은 크기에서 장식선을 추가하지 않는다.
- 텍스트 라벨 없이도 서로 구분되되 내비게이션에서는 라벨을 항상 함께 사용한다.

### 11.2 승인 아이콘

- 앱: 체크와 세 개의 기록 행을 결합한 `Daily Log`
- 운동: 손과 발이 같은 바닥선에 닿는 중간 높이의 `Balanced push-up`
- 클라이밍: 행보드에 걸린 네 손가락과 옆으로 드러난 엄지

기준 SVG 경로는 다음과 같다.

```text
app viewBox 0 0 64 64
M19 22l4 4 8-9 M35 22h10 M19 35h7 M32 35h13 M19 46h7 M32 46h13

workout viewBox 0 0 24 24
circle 19 9 1.5
M17.5 11L3.5 19 M13 13.5l2.5 5.5 M2 19h20

climbing viewBox 0 0 24 24
rect 3 4 18 6 rx 2
M6 10v5 M10 10v7 M14 10v7 M18 10v5 M6 15c0 4 3 6 7 6s6-2 6-6
M18 14c3-2 5-1 4 1l-3 3
```

## 12. 상태와 피드백

| 상태 | 표현 |
|---|---|
| 기본 | 빈 체크 원, 제목, 선택적 카테고리 점 |
| 완료 | 채운 체크와 체크 표시, 완료 요약 |
| 건너뜀 | `건너뜀` 라벨과 취소선 없는 약한 텍스트 |
| 반영 중 | 작은 진행 표시와 `반영 중` |
| 저장 실패 | 오류 아이콘, 원인 요약, `다시 시도` |
| 비활성 | 낮은 대비와 비활성 이유 설명 |
| 키보드 초점 | 2px 초점선과 2px 간격 |

- 성공 토스트를 반복해서 띄우지 않는다.
- 파괴적 행동은 대상과 영향을 문장으로 확인한다.
- 빈 상태는 작은 전용 아이콘, 한 줄 안내, 핵심 버튼 하나만 사용한다.
- 선택 카드의 기본 라디오 원은 숨기고 선택된 카드 외곽선만 강조한다.

## 13. 접근성

- 일반 텍스트는 WCAG AA 대비 4.5:1 이상, 큰 텍스트와 아이콘은 3:1 이상을 목표로 한다.
- 색상만으로 상태, 카테고리, 운동·휴식을 구분하지 않는다.
- 터치 대상은 최소 44×44px이다.
- 모든 아이콘 버튼에는 접근 가능한 이름을 제공한다.
- 키보드만으로 날짜 이동, 할 일 완료, 빠른 추가, 기록 저장이 가능해야 한다.
- 포커스를 모달 안에 가두고 닫은 뒤 원래 제어 요소로 돌려보낸다.
- 브라우저 확대 200%와 동작 감소 설정을 검증한다.
- 타이머는 시각·소리·지원 기기의 진동 중 둘 이상의 채널을 사용할 수 있어야 한다.

## 14. 구현 규칙

- 색상, 간격, 글꼴, 반경, 그림자는 `packages/design-tokens`에서 공유한다.
- 컴포넌트 안에 원시 색상 값을 반복해서 작성하지 않는다.
- 라이트·다크 테마는 같은 의미 토큰 이름에 다른 값을 연결한다.
- 웹과 향후 모바일 앱은 의미 토큰과 아이콘 원본을 공유한다.
- 새 컴포넌트는 기본·선택·완료·비활성·로딩·오류·키보드 초점 상태를 함께 구현한다.
- 구현이 이 문서와 달라져야 하면 코드보다 `docs/PRD.md`와 이 문서를 먼저 수정한다.

## 15. 검증 목록

### 기능 PR

- 변경 화면의 모바일 라이트·다크 상태 확인
- 키보드 초점과 44px 터치 영역 확인
- 로딩·빈 상태·오류 상태 확인
- 변경된 토큰이 다른 화면에 미치는 영향 확인

### develop 병합

- 390px 모바일과 1024px 이상 데스크톱 비교
- 주간·월간 전환과 마지막 보기 기억 확인
- 주간 보기의 모든 날짜에 카테고리별 계획·완료 분포가 올바르게 표시되는지 확인
- 선택 날짜의 항목이 할 일·운동·공부·클라이밍 순서로 그룹화되는지 확인
- 운동 추가에서 자유 운동과 프로그램 참여, 푸시업·풀업 챌린지 선택이 노출되는지 확인
- 공부 추가에서 카테고리별 기록 항목과 단위가 변경되는지 확인
- 운동·공부·클라이밍 탭의 추가 버튼으로 만든 Task가 오늘 화면에 같은 항목으로 표시되는지 확인
- 카테고리·기록 템플릿 저장이 Task를 만들지 않고 일정 추가와 분리되는지 확인
- 오전·오후, 시, 5분 단위 분 선택이 시작·종료 시간에 동일하게 적용되는지 확인
- 날짜별 카테고리 진행률이 완료 수와 전체 수 비율만큼 채워지는지 확인
- 월간 날짜 칸은 무채색을 유지하고 완료 카테고리 색상 막대와 선택 차콜 배경만 표시하는지 확인
- 다크 테마의 주간·월간 선택 날짜가 밝은 면으로 렌더링되지 않는지 확인
- 이전·다음 달 이동과 오늘 복귀 후 연·월 제목, 날짜 수, 시작 요일과 선택 날짜 제목이 일치하는지 확인
- 모바일에서 중복 날짜, 떠 있는 뒤로 가기, 공통 추가 버튼과 중복 설정 버튼이 없는지 확인
- 오늘 화면 본문에 `TODAY`·`오늘` 제목과 브랜드 보조 문구가 중복 노출되지 않는지 확인
- 도메인 탭의 반복 제목이 제거되고 첫 영역부터 내용이 시작되는지 확인
- 도메인 최상단의 월 이동, 월별 요약과 추가·지난 기록 행동이 결합되고 오늘 항목에 체크 원이 표시되는지 확인
- 운동·공부·클라이밍 사이에서 선택 월이 유지되고 월 이동 시 집계 수치가 변경되는지 확인
- 월별 집계가 둘째 줄 전체 폭을 사용하고 상단 행동 버튼 높이가 집계 영역보다 작게 유지되는지 확인
- 879px에서도 달력 외곽선과 선택 날짜 외곽선, 하단 여백이 유지되는지 확인
- 데스크톱 달력 날짜 7열이 할 일 영역을 침범하지 않는지 확인
- 기록 저장 후 반영 중·완료·재시도 상태 확인

### release

- 실제 iPhone Safari와 홈 화면 설치 모드 확인
- 라이트·다크·시스템 테마 전환 확인
- 200% 확대와 동작 감소 설정 확인
- 클라이밍 타이머 숫자, 콜론, 현재·다음 구간의 원거리 가독성 확인
- 소리·지원 기기의 진동·화면 잠금 방지의 허용·거부 상태 확인
